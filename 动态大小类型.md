# 动态大小类型（dynamic Sized Type，DST）

DST指编译期无法确定大小的类型，指向DST的指针一般为**胖指针**（含有大小信息）

* 只能通过指针来间接创建和操作DST类型数据（`&[T]`、`Box<[T]>`可以，`[T]`不可以）；
* DST类型在堆上分配内存，不能作为函数参数和返回值类型；
* `enum`中不能包含DST类型；
* `struct`中只有最后一个元素可以是DST类型，如果包含DST类型，那么这个`struct`也成为DST类型；